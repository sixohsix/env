#!/bin/bash

# USAGE
#   git recent-branches
#
# Enumerate branches that you've recently modified.

git for-each-ref --sort=-committerdate refs/heads/ \
  '--format=%(refname:short)' \
  | perl -ne 'print "$. $_"'
